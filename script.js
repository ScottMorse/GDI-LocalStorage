// select menuForm, menuChecks, ordersList

// define empty items and orders

// starting localStorage data (items,orders)

// set localStorage data if doesn't exist

// event listeners for menuChecks, set them to checked/unchecked

// function checkItem()

// function submitOrder(e)

// function displayOrders()

// event listener for submitting menuForm